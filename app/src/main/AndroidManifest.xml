<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cse10">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">//for increasing performance
        <activity android:name=".CGPA_Calculator"></activity>
        <activity
            android:name=".Help"
            android:parentActivityName=".MainMenu" />
        <activity
            android:name=".AddBook"
            android:parentActivityName=".BookArchiveMenu" />
        <activity
            android:name=".BookArchiveMenu"
            android:parentActivityName=".MainMenu" />
        <activity android:name=".FullRoutine" />
        <activity android:name=".AboutUs" />
        <activity android:name=".ClassRoutine" />
        <activity
            android:name=".AddNotice"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".NoticeMenu" />
        <activity android:name=".Profile" />
        <activity android:name=".MainMenu" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>